import 'package:flutter/material.dart';

Widget spaceHeight({double? height}) {
  return SizedBox(
    height: height,
  );
}

Widget spaceWidth({double? width}) {
  return SizedBox(
    width: width,
  );
}
